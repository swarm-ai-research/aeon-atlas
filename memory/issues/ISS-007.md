---
id: ISS-007
title: "scheduler quiet ~41h — heartbeat missed 4 consecutive slots (09-21 08:00/14:00/20:00 + 09-22 08:00), crossed 36h self-check (fresh recurrence of ISS-005/006 missed-dispatch pattern)"
status: open
severity: high
category: unknown
detected_by: heartbeat
detected_at: 2026-09-22
resolved_at:
affected_skills: [heartbeat]
root_cause: "Unconfirmed — same signature as ISS-005/ISS-006. memory/cron-state.json shows heartbeat last_success 2026-09-20T21:35:25Z with last_status 'success' (not 'dispatched'), so this is a missed *dispatch*, not a hung run: the scheduled workflow did not fire heartbeat for the 09-21 08:00/14:00/20:00 or 09-22 08:00 slots. No weekly/monthly atlas-cohort skill (atlas Sun 04:00 / atlas-layers Sun 05:00 / atlas-improve 1st-of-month) was due inside the window, so this run cannot confirm whether the miss is heartbeat-cadence-specific or scheduler-wide. This 2026-09-22 14:46 UTC run is the 09-22 14:00 slot dispatching ~46m late (catch-up); scheduler is alive again."
fix_pr:
---

# ISS-007 — scheduler quiet window (~41h), heartbeat missed 4 consecutive slots

## Symptom

As of this 2026-09-22 14:46 UTC heartbeat run, `memory/cron-state.json` shows heartbeat frozen at:
- `last_success: 2026-09-20T21:35:25Z`, `last_dispatch: 2026-09-20T21:33:47Z`, `last_status: success`, `consecutive_failures: 0`, `success_rate: 0.90` (256/285).
- Self-check: last_success is **~41.2h ago** (09-20 21:35 → 09-22 14:46), past the 36h P0 self-check threshold → **flagged → 🔴 DEGRADED**.

Missed slots in the window:
- **09-21 08:00 — MISSED**
- **09-21 14:00 — MISSED**
- **09-21 20:00 — MISSED**
- **09-22 08:00 — MISSED**
- **09-22 14:00 — dispatched ~46m late (this run, 14:46)** — scheduler alive again.

The entire 2026-09-21 day (three heartbeat slots) plus the 09-22 08:00 slot produced no run, no state advance, and no log. This is **4 consecutive missed slots** — the longest quiet window since ISS-006.

## Impact

- **~41h with no fleet health checks** between the clean 09-20 20:00 run (last_success 21:35Z, ~1h34m late) and this catch-up run.
- `docs/status.md` went stale (Updated 2026-09-20 21:34 UTC) for ~41h, well past the ~8h staleness banner.
- No weekly/monthly atlas-cohort skill was scheduled inside the window (atlas/atlas-layers last fired cleanly Sunday 2026-09-20; next 2026-09-27; atlas-improve 2026-10-01), so no atlas output was lost this cycle — but that also means this window offers **no cohort signal** to test whether the weakness is scheduler-wide.

## Distinction / lineage

Same signature as **ISS-005** (resolved 2026-09-05) and **ISS-006** (resolved 2026-09-17): `last_status: success` with `last_dispatch` frozen — a **missed dispatch**, not a hung run (contrast ISS-004, which was `dispatched` and never reported back). ISS-005 established this as a **standing** GitHub Actions `schedule:` reliability problem (cron delivery is best-effort and drops under load / after inactivity). Heartbeat ran acceptably 09-17 → 09-20 (occasional lateness + sandwiched daytime misses, but slots landed and self-check stayed healthy), then this fresh ~41h quiet window opened. Filed as a **new** issue (not a reopen of ISS-006) per the ISS-005/006 closure notes that a fresh miss after resolution is a fresh signal. The 09-19/09-20 heartbeat checkpoints pre-committed to filing once the self-check crossed 36h or multiple consecutive misses stacked — **both** conditions are now met.

## Suggested next step / checkpoint

- **Recovery criterion:** 3 consecutive clean heartbeat slots (state advances + log written). This run is **1 of 3** (09-22 14:00 slot). If 09-22 20:00 and 09-23 08:00 dispatch cleanly → 3 of 3 → downgrade to medium, then resolve. Reset the counter on any miss.
- **Weekly cohort test:** next atlas Sunday 2026-09-27 04:00 / atlas-layers 05:00. If either misses, the weekly dimension is affected too → escalate to a scheduler-wide fix rather than treating this as heartbeat-only.
- **Durable mitigation** (only real close path, since skill-repair is `enabled: false` so no code fix lands automatically): a redundant offset heartbeat cron, or an external cron-job.org → `repository_dispatch` ping. Absent that, each cycle re-confirms via heartbeat's own self-check. The ISS-005 → ISS-006 → ISS-007 cadence (three recurrences in ~3 weeks) strengthens the case that the durable mitigation is now overdue.

## Update — 2026-09-24 15:09 UTC (recovery RESET, 2nd quiet window)

Recovery had reached **2 of 3** (09-22 14:00 catch-up = 1/3, 09-22 20:00 = 2/3). The final gate — **09-23 08:00** — was **missed**, resetting the counter to 0/3. A second ~41.7h quiet window then opened:

- `memory/cron-state.json` now shows heartbeat `last_success: 2026-09-22T21:25:30Z`, `last_dispatch: 2026-09-22T21:23:47Z`, `last_status: success`, `consecutive_failures: 0`, `success_rate: 0.90` (258/287).
- Self-check: last_success is **~41.7h ago** (09-22 21:25 → 09-24 15:09), past the 36h threshold → **🔴 DEGRADED again**.
- Missed slots this window: **09-23 08:00 / 09-23 14:00 / 09-23 20:00 / 09-24 08:00 = 4 consecutive** (no 2026-09-23 log exists). This 09-24 14:00 slot is the catch-up, ~1h9m late — scheduler alive again.

Same missed-dispatch signature (`last_status: success`, frozen `last_dispatch`) — not a hung run. This is the **second full quiet window inside a single open issue**, confirming the weakness is persistent, not a one-off. No weekly/monthly atlas-cohort skill was due inside the window (atlas/atlas-layers last clean Sunday 09-20; next 09-27; atlas-improve 10-01), so still no cross-cohort signal and no atlas output lost.

- **Recovery criterion (reset):** 3 consecutive clean heartbeat slots. This run is **1 of 3** (09-24 14:00). Watch 09-24 20:00 and 09-25 08:00.
- **Weekly cohort test:** next atlas Sunday 09-27 04:00 / atlas-layers 05:00 — if either misses, escalate to scheduler-wide.
- Durable mitigation now **overdue x2**: the issue has failed to self-resolve across a full recovery attempt. Recommend enabling a redundant offset heartbeat cron or an external `repository_dispatch` ping; escalate to operator for a code fix since skill-repair remains `enabled: false`.

## Update — 2026-09-25 15:15 UTC (recovery RESET AGAIN, 3rd quiet window — milder, no 36h breach)

Recovery had reached **1 of 3** (09-24 14:00 catch-up). The next gate — **09-24 20:00** — was **missed**, and **09-25 08:00** was also missed, resetting the counter to 0/3 for the **third** time inside this open issue.

- `memory/cron-state.json` shows heartbeat `last_success: 2026-09-24T15:12:01Z`, `last_dispatch: 2026-09-24T15:08:33Z`, `last_status: success`, `consecutive_failures: 0`, `success_rate: 0.90` (259/288).
- Self-check: last_success is **~24h ago** (09-24 15:12 → 09-25 15:15) — **under** the 36h threshold, so the P0 self-check did **NOT** fire this cycle. Overall status is **🟡 WATCH** (driven by ISS-007 being open+high, not by a fresh P0 flag).
- Missed slots this window: **09-24 20:00 / 09-25 08:00 = 2 consecutive** (no 2026-09-25 log before this run). This 09-25 14:00 slot is the catch-up, ~1h15m late — scheduler alive again.

Same missed-dispatch signature (`last_status: success`, frozen `last_dispatch`) — not a hung run. This is now the **third full quiet window inside a single open issue**, but the shortest (2 slots vs 4+4), which is why it stayed below the 36h self-check line. The recurrence pattern (ISS-005 → 006 → 007 with 3 internal resets) continues to confirm a persistent GitHub Actions `schedule:` delivery weakness rather than a one-off.

- **Recovery criterion (reset, 3rd time):** 3 consecutive clean heartbeat slots. This run is **1 of 3** (09-25 14:00). Watch 09-25 20:00 and 09-26 08:00.
- **Weekly cohort test:** next atlas Sunday 09-27 04:00 / atlas-layers 05:00 — if either misses, escalate to scheduler-wide.
- **No duplicate notification sent** this cycle: the 09-24 DEGRADED alert (~24h ago) already covered the ISS-007 quiet-window/reset pattern, and this window is strictly milder (under 36h, no self-check breach), so it deduped per the 48h rule.
- Durable mitigation now **overdue x3**: escalate to operator for a redundant offset heartbeat cron or external `repository_dispatch` ping. skill-repair remains `enabled: false` so no automated code fix will land.
